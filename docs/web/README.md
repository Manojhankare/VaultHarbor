# Web Vault (Optional / TBD)

**Status:** Not planned yet.

A future web vault would also use **client-side decryption** (same crypto contract as the extension). The backend would need no schema changes — only a new origin in `CORS_ALLOWED_ORIGINS`.

## If built later, document here

- `PROJECT_STRUCTURE.md` — frontend stack & folder layout
- `DEPLOYMENT.md` — hosting (Vercel, Netlify, etc.)
- `AUTH.md` — how web client uses same JWT API

## Shared crypto contract

[Client crypto](../backend/CLIENT_CRYPTO.md)
